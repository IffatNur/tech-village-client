import React from 'react';
import useTitle from '../../Title/Title';

const Payment = () => {
    useTitle("Payment");
    return (
        <div>
            
        </div>
    );
};

export default Payment;